global:
  scrape_interval: 30s
  scrape_timeout: 10s
  external_labels:
    environment: home
    project: linux-monitoring-gpt

scrape_configs:
  - job_name: node-exporter
    static_configs:
      - targets:
          - 127.0.0.1:9100

# Phase 2で、ここへAMP向けのremote_write設定を追加する。
