# リポジトリ運用ルール

## 目的と優先順位

- `PROJECT_BRIEF.md` をプロジェクトの目的、設計方針、段階的計画の基準とする。
- PoCでは最初のマイルストーン（AMP上で対象ホストの `up` を確認）を優先し、過剰設計を避ける。
- Node ExporterはLinuxホストのsystemdサービス、Prometheus AgentはDocker Composeで運用する方針とする。変更が必要な場合は理由と影響を先に説明する。

## AWSとセキュリティ

- AWSリソースを作成・変更・削除する前に、対象、料金要因、必要なIAM権限、削除方法を説明して確認を得る。
- AWSの現行仕様、料金、対応リージョン、IAMアクションは公式ドキュメントで確認する。
- IAMは最小権限とし、AMP書き込み用と診断APIのAMP読み取り用の権限を分離する。
- Node Exporter、Prometheus、Grafanaをインターネットへ公開しない。ローカル待受、外向きHTTPS、読み取り専用APIを基本とする。
- 任意のPromQL、シェルコマンド、無制限のホスト名を外部入力として受け付けない。

## 秘密情報とGit

- アクセスキー、シークレット、APIキー、OAuthクライアントシークレット、秘密鍵、実値入りの`.env`をコミットしない。
- 例示用には `env.example` を使い、実値は記載しない。
- Prometheus実データ、ログ、Terraform stateをコミットしない。
- 既存のユーザー変更を削除・上書きしない。

## 実装と検証

- 変更は小さな段階に分け、変更ごとに確認コマンドまたはテストを示す。
- 新しい構成や運用方法を導入する際は、目的、仕組み、確認方法を日本語で短く説明する。
- 実装と同時にREADMEまたは関連ドキュメントを更新する。
