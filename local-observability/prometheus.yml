global:
  scrape_interval: 30s
  scrape_timeout: 10s
  external_labels:
    environment: home
    project: linux-monitoring-gpt
    host_id: home-server

scrape_configs:
  - job_name: node-exporter
    static_configs:
      - targets:
          - 127.0.0.1:9100
